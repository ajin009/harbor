<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset Request</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        .email-container {
            max-width: 600px;
            margin: 20px auto;
            padding: 40px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .email-header {
            padding: 20px 0;
        }

        .email-header h2 {
            margin: 0;
            color: #333;
            font-size: 24px;
            font-family: ui-sans-serif;
            font-weight: 100;
        }

        .email-body {
            padding: 20px;
            color: #555;
            margin: 20px 0;
            font-size: 16px;
        }

        .reset-button {
            display: inline-block;
            padding: 12px 24px;
            margin: 20px 0;
            background: #007cff8a;
            color: #fff !important;
            text-decoration: none;
            border: none;
            font-size: 16px;
            font-weight: bold;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: background 0.3s ease;
        }

        .reset-button:hover {
            background: #007cff8a;
        }

        .email-footer {
            font-size: 12px;
            color: #888;
            margin-top: 20px;
        }
    </style>
</head>

<body>
    <div class="email-container">
        <div class="email-header">
            <h2>Password Reset Request</h2>
        </div>
        <div class="email-body">
            <img src="cid:passwordImage" alt="Password Reset" style="max-width: 25%; height: auto;">
            <p>Hi {{name}},</p>
            <p>We received a request to reset your password. Please click the button below to reset your password within
                the next 1 hour</p>
            <a href="{{resetLink}}" class="reset-button">Reset Password</a>
            <p>If you did not request this, please ignore this email.</p>
        </div>
        <div class="email-footer">
            <p>©{{currentYear}} CodesLand. All rights reserved.</p>
        </div>
    </div>
</body>

</html>